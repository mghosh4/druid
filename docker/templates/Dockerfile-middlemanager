FROM xiaoyao1991/druid-base:latest

RUN echo "[supervisord]\n\
nodaemon=true\n\
loglevel=debug\n\
[program:druid-middlemanager]\n\
user=root\n\
command=java\n\
  -Xmx256m\n\
  -Duser.timezone=UTC\n\
  -Dfile.encoding=UTF-8\n\
  -classpath 'conf/druid/_common:conf/druid/middleManager:lib/*'\n\
  io.druid.cli.Main server middleManager\n\
priority=1" > /etc/supervisor/conf.d/supervisord.conf

ENTRYPOINT export HOSTIP="$(resolveip -s $HOSTNAME)" && exec /usr/bin/supervisord -c /etc/supervisor/conf.d/supervisord.conf
